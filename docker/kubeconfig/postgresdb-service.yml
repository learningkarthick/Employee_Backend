apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: employee-namespace   # ✅ Replace with your namespace name
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432