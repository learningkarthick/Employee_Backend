apiVersion: v1
kind: Service
metadata:
  name: employee-backend-service
  namespace: employee-namespace   # ✅ Replace with your namespace name
  labels:
    app: employee-backend
spec:
  selector:
    app: employee-backend
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
